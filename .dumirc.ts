import { defineConfig } from 'dumi';

export default defineConfig({
  themeConfig: {
    name: '',
    socialLinks: { github: 'https://github.com/neo-of-matrix/duckyo' },
  },
  base: '/duckyo/',
  publicPath: '/duckyo/',
});
